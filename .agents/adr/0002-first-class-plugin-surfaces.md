# First-Class Plugin Surfaces

Doctor treats plugin surfaces as first-class product surfaces alongside CLI surfaces. Plugin surfaces can contribute project inventory and runtime evidence, not only decide when Doctor runs. This makes framework-native usage through integrations such as the Vite plugin, Nuxt module, MCP tools, and future ecosystem integrations part of the core architecture rather than wrappers around the CLI.
