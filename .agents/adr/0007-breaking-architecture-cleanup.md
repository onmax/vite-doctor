# Breaking Architecture Cleanup

This architecture cleanup does not preserve backwards compatibility or legacy code when a cleaner Doctor model is available. Public names, export paths, rule IDs, and surface options may change if the new model makes Rule Packs, Doctor Runs, Doctor Extensions, CLI Surfaces, and Plugin Surfaces clearer.
